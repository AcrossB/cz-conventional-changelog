name: Deploy by Push to 'master' branch

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup Node.js
        uses: actions/setup-node@v2.4.0
        with:
          node-version: '16'
      - name: Install Dependencies
        run: npm ci
      - name: Publish Package
        run: npm publish --registry=https://npm.pkg.github.com
        env:
          NODE_AUTH_TOKEN: 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
